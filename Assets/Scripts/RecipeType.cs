using UnityEngine;

public enum RecipeType
{
    OnionSoup,      // Soupe aux oignons (3 oignons découpés)
    TomatoSoup,     // Soupe aux tomates (3 tomates découpées)
    MushroomSoup,   // Soupe aux champignons (3 champignons découpés)
    Burger          // Hamburger (pain, salade découpée, tomate découpée, viande cuite)
}

