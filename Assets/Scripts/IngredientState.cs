using UnityEngine;

public enum IngredientState
{
    Raw,        // Brut
    Cut,        // Découpé (pour oignons, tomates, champignons, salade)
    Chopped,    // Haché (pour viande)
    Cooked      // Cuit
}

